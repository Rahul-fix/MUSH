# Grid search configuration for Gaussian Blur (Aggressive Motion)
project: pepper-segmentation-grid-blur-aggressive
method: grid
parameters:
  image_size:
    values:
      - [640, 360]
  batch_size:
    value: 3
  learning_rate:
    value: 2e-4
  epochs:
    value: 100
  log_freq:
    value: 10
  blur_prob:
    values:
      - 0.5
  blur_kernel:
    values:
      - 15
  blur_sigma:
    values:
      - 1.5
      - 2.0
